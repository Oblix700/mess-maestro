
import {
  Card,
  CardContent,
  CardDescription,
  CardHeader,
  CardTitle,
} from "@/components/ui/card";

export default function GenerateFinalShoppingListPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Generate Final Shopping List</CardTitle>
        <CardDescription>
          Generate the final, supplier-specific shopping lists based on monthly needs and price comparisons.
        </CardDescription>
      </CardHeader>
      <CardContent>
        <p>Placeholder content for generating the final shopping list.</p>
      </CardContent>
    </Card>
  );
}
